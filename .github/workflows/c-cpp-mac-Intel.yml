name: MacOS qmake qt6.2 Universal C/C++ CI

on:
  workflow_dispatch:
      branches: [ master ]
      

jobs:
  build:

    runs-on: macos-12

    steps:
    - name: Install Qt
      uses: jurplel/install-qt-action@v3
      with:
        version: 6.2.*
        modules: 'qtcharts'
        
    - uses: actions/checkout@v3
    - name: qmake
      run: qmake -makefile CaptureStream2.pro QMAKE_APPLE_DEVICE_ARCHS="x86_64 arm64"
    - name: make
      run: make
      
    - name: Make CaptureStream2 executable
      run: chmod -R 755 ./CaptureStream2.app

    - name: mkdir
      run: mkdir ./build
    - name: cp icon
      run: cp ./icon.icns ./CaptureStream2.app/Contents/Resources/
    - name: cp qss
      run: cp ./qss_files/stylesheet-mac.qss ./CaptureStream2.app/Contents/MacOS/
    - name: mv
      run: mv ./CaptureStream2.app ./build/
  
    - uses: actions/upload-artifact@v4
      with:
        name: CaptureStream2-OS10x-Universal
        path: ./build

